package com.gobase.web.conf;
/*
package com.renren.auto.video.web.conf;

import org.springframework.boot.web.servlet.FilterRegistrationBean;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import com.renren.auto.video.web.filter.ResponseFilter;

@Configuration
public class FilterConfig {
	*/
/**
	 * 配置过滤器，这里过滤器主要是对返回值做后继处理
	 * 
	 * @return
	 *//*

	@Bean
	public FilterRegistrationBean someFilterRegistration() {
		FilterRegistrationBean registration = new FilterRegistrationBean();
		registration.setFilter(new ResponseFilter());
		registration.addUrlPatterns("*/
/*");
		registration.addInitParameter("paramName", "paramValue");
		registration.setName("responseFilter");
		return registration;
	}

}
*/
